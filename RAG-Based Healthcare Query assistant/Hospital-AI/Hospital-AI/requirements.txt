# --- Core web framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Data / database ---
pandas>=2.1.0
SQLAlchemy>=2.0.25

# --- SQL safety validation ---
sqlglot>=23.0.0

# --- Multi-agent orchestration ---
langgraph>=0.1.0

# --- RAG: embeddings + vector search ---
faiss-cpu>=1.7.4
scikit-learn>=1.3.0
numpy>=1.26.0
# sentence-transformers is optional (heavy: pulls in torch). Install it and
# set EMBEDDING_BACKEND=sentence-transformers for production-grade semantic
# search. The app runs fully without it using the TF-IDF fallback.
# sentence-transformers>=2.5.0

# --- LLM providers (install whichever you configure via LLM_PROVIDER) ---
anthropic>=0.25.0
openai>=1.14.0
groq>=0.4.0

# --- Frontend ---
streamlit>=1.32.0
requests>=2.31.0

# --- Testing ---
pytest>=8.0.0
pytest-cov>=4.1.0
httpx>=0.27.0

# --- Dev / quality ---
python-dotenv>=1.0.0
